package security;

/**
 * @author Michael Ruf
 * @since 2016-03-11
 */
@SuppressWarnings("unused")
public class SecurityHandler extends Security.Handler {

    static boolean authenticate(Authenticate authenticate) {
        return true; // TODO for now all is fine
    }
}
